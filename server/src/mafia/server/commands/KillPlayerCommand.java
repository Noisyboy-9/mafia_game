package mafia.server.commands;

/**
 * The type Kill player command.
 */
public class KillPlayerCommand implements Command {
    /**
     * Instantiates a new Kill player command.
     */
    public KillPlayerCommand() {
    }

    @Override
    public String toString() {
        return "killClient";
    }
}
